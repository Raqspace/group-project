// BlockchainSimulator - lightweight simulation config used for fake chain behavior.
// Keep this file while the app is in simulation mode (no real blockchain integration).

// Simulated network latency in milliseconds to mimic transaction delays.
export const blockchainSimulator = {
  latencyMs: 250,
};
