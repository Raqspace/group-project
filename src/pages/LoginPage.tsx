// LoginPage() - placeholder for login and auth UI (route: #/login).

export function LoginPage() {
  return (
    <main className="app-shell">
      <section className="content">
        <section className="bottom-row card">
          <h3>Login</h3>
          <p className="live-note">PLACEHOLDER - build your login form here.</p>
          <p className="live-note">
            Use <a href="#/dashboard">#/dashboard</a> to return to the main app.
          </p>
        </section>
      </section>
    </main>
  );
}
