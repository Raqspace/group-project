// NotFoundPage() - fallback page when no route matches.
export function NotFoundPage() {
  return (
    <section className="bottom-row card">
      <h3>Page not found</h3>
      <p className="live-note">Use the sidebar to navigate to an available page.</p>
    </section>
  );
}
